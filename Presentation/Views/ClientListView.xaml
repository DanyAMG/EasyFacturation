<UserControl x:Class="EasyFacturation.Presentation.Views.ClientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <TextBlock Text="Clients" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid ItemsSource="{Binding Clients}" SelectedItem="{Binding SelectedClient}" AutoGenerateColumns="False" CanUserAddRows="False" MouseDoubleClick="DataGrid_MouseDoubleClick"
                  IsReadOnly="True" Margin="0,40,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="First Name" Binding="{Binding DisplayName}" />
                <DataGridTextColumn Header="Company" Binding="{Binding CompanyName}" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" />
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" />
                <DataGridTextColumn Header="Phone" Binding="{Binding DisplayAdress}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>