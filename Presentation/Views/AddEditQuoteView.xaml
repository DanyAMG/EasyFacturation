<UserControl x:Class="EasyFacturation.Presentation.Views.AddEditQuoteView"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EasyFacturation.Presentation.Views">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Titre de la page et message d'erreur -->
            <StackPanel Orientation="Vertical" Grid.Row="0" HorizontalAlignment="Center">
                <TextBlock Text="Add a new Quote" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red"/>
            </StackPanel>

            <TextBlock Text="Client"/>
            <ComboBox ItemsSource="{Binding Clients}" 
                SelectedItem="{Binding CurrentQuoteClient, Mode=TwoWay}"
                DisplayMemberPath="FullName" 
                Margin="0,0,0,10"/>
            <Button Content="New Client" Command="{Binding AddClientCommand}" Margin="0,0,0,10"/>

            <!-- Formulaire -->
            <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" Width="300" >
                <!-- Title -->
                <TextBlock Text="Title"/>
                <ComboBox SelectedItem="{Binding Title}" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,20,0,0">

                    <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Width="100"
                    Margin="0,0,10,0"/>


                    <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Width="100"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
