<UserControl x:Class="EasyFacturation.Presentation.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Height="400" Width="600">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Devis en cours." FontSize="20" FontWeight="Bold" Grid.Column="0"/>
        <TextBlock Text="Factures en cours." FontSize="20" FontWeight="Bold" Grid.Column="1"/>

        <TextBlock Text="Devis 1" Grid.Row="1" Grid.Column="0"/>
        <TextBlock Text="Facture 1" Grid.Row="1" Grid.Column="1"/>

        <TextBlock Text="Devis 2" Grid.Row="2" Grid.Column="0"/>
        <TextBlock Text="Facture 2" Grid.Row="2" Grid.Column="1"/>
    </Grid>
</UserControl>
